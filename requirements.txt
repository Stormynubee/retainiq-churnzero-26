# RetainIQ — pinned dependencies
# Tested on Python 3.11+ (Python 3.14 user-install also works).
# If you hit a wheel-build error on 3.14, drop to a 3.11 / 3.12 venv.

# Core
numpy>=1.26
pandas>=2.1
scikit-learn>=1.4
scipy>=1.11

# Boosting
lightgbm>=4.3
catboost>=1.2

# Calibration & explainability
shap>=0.45

# Causal / uplift
causalml>=0.15
# econml is an alternative; uncomment if causalml install fails on Windows
# econml>=0.15

# Counterfactuals
dice-ml>=0.11

# Fairness
fairlearn>=0.10

# Hyperparameter optimization
optuna>=3.5

# Plotting (deck visuals)
matplotlib>=3.8
seaborn>=0.13

# Notebook + utilities
jupyter>=1.0
ipykernel>=6.29
tqdm>=4.66
joblib>=1.3
pyarrow>=15.0
